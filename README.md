# byte

Your personal AI agent. Tell it what to do — it gets it done.

---

## Setup

```bash
npm install
cp server/.env.example server/.env
# fill in your API keys
```

## Run

```bash
# backend
cd server && npm run dev

# frontend
cd frontend && npm run dev
```

Open [http://localhost:5173](http://localhost:5173)
